#include <stdio.h>
/**
 * main - this is the function of task three
 * Return: always 0
 */
int main(void)
{
char i;
for (i = 'a'; i <= 'z'; i++)
putchar(i);
{
if (i == 'e' || i == 'q')
putchar(i);
else
putchar(i);
}
putchar('\n');
return (0);
}
